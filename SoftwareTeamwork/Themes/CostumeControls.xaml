<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SoftwareTeamwork">
    <!--Controls-->

    <!--DcontextMenu-->

    <!--<Style x:Key="DContextMenuS" TargetType="{x:Type local:DContextMenu}">
        <Setter Property="Placement" Value="Mouse"/>
        <Setter Property="Padding" Value="-1"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="{DynamicResource VsBorderColor}" BlurRadius="5" ShadowDepth="0"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DContextMenu}">
                    <Border x:Name="RootBorder" BorderThickness="2" BorderBrush="{TemplateBinding BorderBrush}" Margin="{TemplateBinding Padding}" >
                        <StackPanel Background="{DynamicResource VsBackGroundBrush}">
                            <local:DMenuItem x:Name="M1" Icon="&#xE15E;" Command="{StaticResource OpenMainWindowCommand}" 
                         IconColorN="{DynamicResource VsBorderColor}" IconMask="{DynamicResource VsBorderColor}"
                         IconColorR="White"  Header="{Binding OpenOrClose, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=local:DContextMenu},Mode=OneWay}" CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DContextMenu}}" />
                            <local:DMenuItem x:Name="M2" Icon="&#xEA39;" Header="设置" Command="{StaticResource ChangeTheme}"/>
                            <local:DMenuItem x:Name="M3" Icon="&#xEA39;" Header="选项" />
                            <local:DMenuItem x:Name="M4" Icon="&#xEA39;" Header="近期流量数据" />
                            <local:DMenuItem x:Name="M5" Icon="&#xEA39;" Header="天气情况" />
                            <local:DMenuItem x:Name="M6" Icon="&#xEA39;" Header="课程表" />
                            <Separator Style="{StaticResource SeparatorStyle1}" />
                            <local:DMenuItem x:Name="M10" Icon="&#xEA39;" Header="退出程序" 
                         IconColorN="Red"
                         IconMask="Red"
                         IconColorR="White"
                         Command="{StaticResource ClsCommand}"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <local:DContextMenu x:Key="DcontextMenu" Placement="MousePoint" Background="{DynamicResource VsBackGroundBrush}">
        <local:DMenuItem x:Name="M1" Icon="&#xE15E;" Command="{StaticResource OpenMainWindowCommand}" 
                         IconColorN="{DynamicResource VsBorderColor}" IconMask="{DynamicResource VsBorderColor}"
                         IconColorR="White"  Header="{Binding OpenOrClose, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=local:DContextMenu},Mode=OneWay}" CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DContextMenu}}" />
        <local:DMenuItem x:Name="M2" Icon="&#xEA39;" Header="设置" Command="{StaticResource ChangeTheme}"/>
        <local:DMenuItem x:Name="M3" Icon="&#xEA39;" Header="选项" />
        <local:DMenuItem x:Name="M4" Icon="&#xEA39;" Header="近期流量数据" />
        <local:DMenuItem x:Name="M5" Icon="&#xEA39;" Header="天气情况" />
        <local:DMenuItem x:Name="M6" Icon="&#xEA39;" Header="课程表" />
        <Separator Style="{StaticResource SeparatorStyle1}" />
        <local:DMenuItem x:Name="M10" Icon="&#xEA39;" Header="退出程序" 
                         IconColorN="Red"
                         IconMask="Red"
                         IconColorR="White"
                         Command="{StaticResource ClsCommand}"/>
    </local:DContextMenu>

    <!--FlowContentPanel-->

    <Border x:Key="FlowContentPanel" Width="300" Height="80" BorderThickness="1" Margin="3">
        <Border.BorderBrush>
            <SolidColorBrush Color="{DynamicResource VsBorderColor}"/>
        </Border.BorderBrush>
        <Border.Effect>
            <DropShadowEffect Color="{DynamicResource VsBackGroundColor}" Opacity="{Binding Shadow, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=local:DPopup}}" BlurRadius="5" ShadowDepth="0" />
        </Border.Effect>
        <Grid>
            <Grid.Background>
                <SolidColorBrush Color="{DynamicResource VsBackGroundColor}"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="Title" Text="{Binding Title,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DPopup}}" FontSize="14" Foreground="{DynamicResource NotifyTextColor_L1_N}" FontWeight="UltraLight"  VerticalAlignment="Center" Grid.Column="1" />
            <TextBlock x:Name="Content" Text="{Binding Content,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DPopup}}" FontSize="12" Foreground="{DynamicResource NotifyTextColor_L2_N}" FontWeight="Thin" VerticalAlignment="Center" Grid.Column="1" Grid.Row="1" />
            <Grid Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.RowSpan="2">
                <Path Data="{Binding IconPath, RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=local:DPopup},Converter={StaticResource StrToPathConverter}}" Stroke="{DynamicResource NotifyTextColor_L1_N}" StrokeThickness="5">
                </Path>
                <Ellipse Width="50" Height="50" Canvas.Left="5" Canvas.Top="5" Stroke="{DynamicResource NotifyTextColor_L1_N}" StrokeThickness="0.8"/>
                <TextBlock Text="{Binding Percent,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:DPopup}}" Style="{DynamicResource NumberTextBlock}"/>
            </Grid>
        </Grid>
    </Border>

</ResourceDictionary>